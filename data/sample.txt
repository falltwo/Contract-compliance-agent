這是一份示範文件，用來測試 RAG（檢索增強生成）。

## Pinecone
Pinecone 是一個向量資料庫，常用於儲存 embedding 向量並做相似度檢索。

## RAG 流程（簡化）
1. 將文件切成多個 chunk。
2. 使用 embedding 模型把每個 chunk 轉成向量。
3. 把向量與 chunk 內容一起寫入向量資料庫。
4. 使用者提問時，先把問題轉成向量，再去向量資料庫做相似度查詢。
5. 把最相關的 chunk 組成「檢索內容」提供給 LLM 生成答案。

